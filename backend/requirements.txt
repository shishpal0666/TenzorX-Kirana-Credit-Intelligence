# TenzorX Kirana Underwriting Backend Dependencies
# All pure-Python — works on Python 3.14+

# Web framework
flask>=3.0.3
flask-cors>=4.0.1
python-dotenv>=1.0.1
Werkzeug>=3.0.3

# HTTP client (for Gemini REST API)
requests>=2.31.0

# EXIF reading (pure Python, no C extensions)
exifread>=3.0.0

# Production WSGI server (for Render/Railway/Heroku)
gunicorn>=21.2.0
